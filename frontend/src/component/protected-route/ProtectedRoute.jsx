// import { Navigate } from "react-router-dom";

// // eslint-disable-next-line react/prop-types
// const ProtectedRoute = ({ isAllowed, requiredRole, children }) => {
//   if (!isAllowed) {
//     // Redirect to the login page if not allowed
//     return <Navigate to="/login" replace />;
//   }

//   // Check if the user has the required role (admin in this case)
//   if (requiredRole && isAllowed !== requiredRole) {
//     // Redirect to a forbidden page or home if the user doesn't have the required role
//     return <Navigate to="/login" replace />;
//   }

//   // Render the protected component if allowed
//   return children;
// };

// export default ProtectedRoute;

